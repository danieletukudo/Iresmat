version: '3.8'

services:
  web:
    build: .
    ports:
      - "5001:5001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - FLASK_ENV=production
    volumes:
      - ./materials_list.txt:/app/materials_list.txt:ro
    restart: unless-stopped

